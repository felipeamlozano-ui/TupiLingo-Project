from django.apps import AppConfig

class WorldBuilderConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'world_builder'
    verbose_name = 'World Builder CMS Enterprise'
